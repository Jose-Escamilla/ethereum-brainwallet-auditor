# Sintaxis para Linux/Mac (shell bash), pegarla en el terminal y copiar el resultado

python - <<'PY'
import secrets
print(secrets.token_hex(32))  # imprime 64 caracteres hexadecimales
PY

# Sintaxis para Windows (PowerShell), pegarla en el PowerShell y copiar el resultado
python
import secrets
print(secrets.token_hex(32))
